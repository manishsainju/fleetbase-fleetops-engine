import geojsonLineString from 'dummy/utils/geojson/line-string';
import { module, test } from 'qunit';

module('Unit | Utility | geojson/LineString', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    let result = geojsonLineString();
    assert.ok(result);
  });
});
