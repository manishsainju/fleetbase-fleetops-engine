import geojsonFeatureCollection from 'dummy/utils/geojson/feature-collection';
import { module, test } from 'qunit';

module('Unit | Utility | geojson/FeatureCollection', function () {
    // TODO: Replace this with your real tests.
    test('it works', function (assert) {
        let result = geojsonFeatureCollection();
        assert.ok(result);
    });
});
