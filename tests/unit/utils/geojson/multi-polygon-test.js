import geojsonMultiPolygon from 'dummy/utils/geojson/multi-polygon';
import { module, test } from 'qunit';

module('Unit | Utility | geojson/MultiPolygon', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    let result = geojsonMultiPolygon();
    assert.ok(result);
  });
});
