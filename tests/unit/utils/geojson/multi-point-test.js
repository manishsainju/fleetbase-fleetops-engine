import geojsonMultiPoint from 'dummy/utils/geojson/multi-point';
import { module, test } from 'qunit';

module('Unit | Utility | geojson/MultiPoint', function () {
    // TODO: Replace this with your real tests.
    test('it works', function (assert) {
        let result = geojsonMultiPoint();
        assert.ok(result);
    });
});
