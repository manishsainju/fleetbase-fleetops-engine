import extend from 'dummy/utils/extend';
import { module, test } from 'qunit';

module('Unit | Utility | extend', function () {
    // TODO: Replace this with your real tests.
    test('it works', function (assert) {
        let result = extend();
        assert.ok(result);
    });
});
